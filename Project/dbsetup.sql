--  NOTE Creates a table in the DB
  -- CREATE TABLE IF NOT EXISTS cats (
--   id int NOT NULL AUTO_INCREMENT, 
--   name VARCHAR(255) NOT NULL, 
--   age int,
--     PRIMARY KEY (ID)
--   );
  -- NOTE  Adding record to DB
  -- INSERT INTO cats (name, age)
  -- VALUES ("Garfield", 41);

  -- DELETE FROM cats WHERE id = 2;
  -- NOTE select all files for cats
  -- SELECT * FROM cats
  -- NOTE select "id" & "name" from cats
  -- SELECT id, name FROM cats

  -- NOTE Update record
  -- UPDATE cats SET 
  -- name = Sylvester Jr.
  -- WHERE id = 5;
  
    NOTE modifies table specifics or